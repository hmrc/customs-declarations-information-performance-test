
# This file is read when runLocal = false in application.conf (default is false)

  services {

    auth {
      protocol = http
      host = customs-api-hods-stubs.protected.mdtp
      port = 80
    }

    customs-declarations-information {
      protocol = http
      host = customs-declarations-information.protected.mdtp
      port = 80
    }

  }
